<div id="fb-root"></div>
<script>
  yayo = null;

  // Additional JS functions here
  window.fbAsyncInit = function() {
    FB.init({
      appId      : '327217574049864', // App ID
      channelUrl : '//www.valydation.com/channel.html', // Channel File
      status     : true, // check login status
      cookie     : true, // enable cookies to allow the server to access the session
      xfbml      : true  // parse XFBML
    });

    // Additional init code here

  };

  // Load the SDK Asynchronously
  (function(d){
     var js, id = 'facebook-jssdk', ref = d.getElementsByTagName('script')[0];
     if (d.getElementById(id)) {return;}
     js = d.createElement('script'); js.id = id; js.async = true;
     js.src = "//connect.facebook.net/en_US/all.js";
     ref.parentNode.insertBefore(js, ref);
  }(document));


  function fbLogin() {
    FB.login(function(response) {
        if (response.authResponse) {
          yayo = response;
          $('#fb_id')[0].value = response.authResponse.userID;
          $('#fb_access_token')[0].value = response.authResponse.accessToken;
          $('#fb_login').submit();
        } else {
          alert('cancel');
        }
    }, {scope: 'email'});
  }

</script>



<style type="text/css">
      body {
        padding-top: 80px;
        padding-bottom: 40px;
        
        
      }

      .form-signin {
        max-width: 300px;
        padding: 19px 29px 29px;
        margin: 0 auto 20px;
        background-color: #f5f5f5;
        border: 1px solid #592A84;
        -webkit-border-radius: 5px;
           -moz-border-radius: 5px;
                border-radius: 5px;
        -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
           -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                box-shadow: 0 1px 2px rgba(0,0,0,.05);
      }
      .form-signin .form-signin-heading,
      .form-signin .checkbox {
        margin-bottom: 10px;
      }
      .form-signin input[type="text"],
      .form-signin input[type="password"] {
        font-size: 16px;
        height: auto;
        margin-bottom: 15px;
        padding: 7px 9px;
		width: 200px;
      }

    </style>


<%= form_tag({:controller => "users", :action => "create"}, :id => "fb_login") do %>
  <input type="hidden" id="fb_id" name="fb_id">
  <input type="hidden" id="fb_access_token" name="fb_access_token">
<% end %>

    <div class="container ">
      <ul class="nav">
        <%= form_tag({:controller => "sessions", :action => "create"}) do %>
        <div class="form-signin">
        <center><p><font size="5">Log In</font></p></center>
        <hr>
        <center>Sign in with</center><br/>
        <ul class="buttons">
				<li style="list-style-type: none;" >
        <a class="facebook BigButton" href="#" onclick="fbLogin()">
        <span class="logo-fb"></span>Facebook</a></li><br>
      </ul>

      <hr>
			<% if flash[:message] %>
			<div class="alert alert-success">
			 <%= flash[:message] %>.
			</div>
			<% end %>
        <% if @error_message %>
        <div class="alert alert-error">  
        <%= @error_message %>
        </div>
        <% end %>

        <center>
          Sign in with your email<br/><br/>
          <input type="text" id="email" name="email" value="<%= params[:email] %>" class="input-block-level" placeholder="Email"><br>
          <input type="password" id="passwd_clear" name="passwd_clear" class="input-block-level" placeholder="Password"><br>
          <button class="btn btn-large btn-success" type="submit">Login</button><br><br>


        

        <% end %>
		
		<%= form_tag({:controller => "users", :action => "forgot_passwd"}) do %>
Forgot your password?
       <input type="text" id="email" name="email" value="<%= params[:email] %>" class="input-block-level" placeholder="Enter Your Email"><br>
<button class="btn btn-large btn-success" type="submit">Submit</button><br>
   <% end %></center>
</div></div><br><br><br><br><br><br>